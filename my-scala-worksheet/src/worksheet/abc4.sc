val lastAnswer = 10 % 3
val lastAnswer = 10 % 1

1^7

// java.lang.ArithmeticException: / by zero
//val lastAnswer = 10 % 0

val arr = Array()
arr.size
arr.length
arr == null
arr.isEmpty

val arr = Array("2", "3")
arr.size
arr.length
arr == null
arr.isEmpty

var outList: List[Int] = List()
outList
var lastAnswer = 0
outList = lastAnswer :: outList
outList